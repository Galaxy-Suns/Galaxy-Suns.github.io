```py


# ---------------trying-----------------
# 时间2021/8/29 1:18
#误触保护模块--------------------------------------------------------------------------------
y=0
x=input('是否确认进行更新日志操作？是请输入yes并按下空格,否则直接按下空格')
print('')
while x!='' and x!='yes':
    print('输入错误，是请输入yes并按下空格,否则直接按下空格')
    print('')
    x=input('请重新输入')
    print('')
if x=='yes':
    for item in range(3):
        pwd = input('请输入密码')
        print('')
        if pwd == '7452kk':
            print('密码正确')
            print('')
            break
        else:
            print('密码不正确')
            print('')
    else:  # 循环正常结束才会执行，而不是被 break 打断结束
        print('对不起，，三次密码均使用错误')
        y=1
        print('')
else:y=1
print('--------------------------------------------------------------------------------')
#------------------------------------------------------------------------------#误触保护模块
#保留模块-------------------------------------------------------------------
'''每次执行该程序得到序号不同'''
if y==0:
    m=0
    n = 0
    for item in range(2):
        for item in range(1):
            if m != 1:
                break
            a = c / 10
            print('本次更新版本  ', a)
            print('')
            fp = open('E:/vippython/chap3/计算器更新日志.py', 'a+')
            print('        c=' + str(c) + '+1', file=fp)
            fp.close
            n = 1
        if n == 1:
            # 输入命名模块-----------------------------------------------------------------------
            fq = open('C:/Users/Administrator/Desktop/植物大战僵尸/计算器更新日志/' + str(a) + '.txt', 'a+')
            print('--------------------------------------------------------------------------------')
            print('')
            e=1
            print('请输入更新日志，按序号依次输入相关内容,若某一序号之后无内容输入，直接按下回车结束')
            print('')
            while e!='':
                b = input(str(e)+'.')
                print('')
                if b=='':
                    break
                print(str(e) + '.' + b, file=fq)
                e+=1
            print('--------------------------------------------------------------------------------')
            fq.close
            # -----------------------------------------------------------------------#输入命名模块
            break
        m = 1
        c = 20

```
